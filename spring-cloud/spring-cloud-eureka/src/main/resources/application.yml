spring:
  application:
    name: dapp-eureka-server
  security:
    user:
      name: sdlzcoin
      password: ZG0bHCl!521
server:
  port: 9001
eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false
    service-url:
      defaultZone: http://${eureka-host}:${server.port}/eureka/
  server:
    enable-self-preservation: false
